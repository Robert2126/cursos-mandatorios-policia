services:
  advisor:
    build:
      context: ..
      dockerfile: api/Dockerfile
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/api/data
    restart: unless-stopped
